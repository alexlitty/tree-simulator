#!/bin/bash
source $(dirname $0)/../config/config.bash
PATH_MAKEFILE=${PATH_CONFIG}/Makefile

TARGET=$(uname -s)_$(uname -p)
if [ $# -gt 0 ]; then
    TARGET=$1
fi

make -f ${PATH_MAKEFILE} ${TARGET}
